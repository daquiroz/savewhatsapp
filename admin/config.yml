# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: "config"
    label: "Config"
    editor:
      preview: false
    files:
      - label: "Organizations"
        name: "organizations"
        file: "_data/organizations.yml"
        fields:
          - label: "Organization Items"
            name: "items"
            widget: "list"
            fields:
              - {label: Name, name: name, widget: string}
              - {label: Country, name: country, widget: string}
              - {label: Website, name: website, widget: string, required: false}
              - {label: Logo, name: logo, widget: image}
              - {label: Featured, name: featured, widget: boolean}
              - {label: Actions_EN, name: actions_en, widget: string, required: false}
              - {label: Actions_ES, name: actions_es, widget: string, required: false}
              - {label: Actions_PT, name: actions_pt, widget: string, required: false}
      - label: "Events"
        name: "events"
        file: "_data/events.yml"
        fields:
          - label: "Event Items"
            name: "items"
            widget: "list"
            fields:
              - {label: Title_EN, name: title_en, widget: string}
              - {label: Title_ES, name: title_es, widget: string}
              - {label: Description_EN, name: description_en, widget: string}
              - {label: Description_ES, name: description_es, widget: string}
              - {label: Active, name: active, widget: boolean}
      - label: "Authorities"
        name: "authorities"
        file: "_data/authorities.yml"
        fields:
          - label: "Authorities Items"
            name: "items"
            widget: "list"
            fields:
              - {label: Name, name: name, widget: string}
              - {label: Position, name: position, widget: string}
              - {label: Country, name: country, widget: string}
              - {label: Party, name: party, widget: string}